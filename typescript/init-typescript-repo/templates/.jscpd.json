{
  "//": "Threshold: fail when more than 5% of tokens are duplicated; ignore globs exclude the install-time byte copies (templates, ci.yml, run-gates.sh), markdown, which the README-fingerprint check in verify-sync.sh owns, and kcov's generated coverage/ report tree, which the shell coverage gate keeps in place after the run.",
  "threshold": 5,
  "ignore": [
    "**/init-*-repo/templates/**",
    "**/ci.yml",
    "**/run-gates.sh",
    "**/coverage/**",
    "**/*.md"
  ]
}
