# Root hygiene workflow

The repo has no root GitHub workflow today. The hygiene workflow for the  repo itself is a new file (.github/workflows/hygiene.yml). Keep it separate from the per-language templates so new repos only inherit what they need.
