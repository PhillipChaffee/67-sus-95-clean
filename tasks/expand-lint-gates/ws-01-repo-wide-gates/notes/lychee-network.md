# Lychee network

lychee needs network access. CI can hit flaky links. Lychee supports retry  and an offline exclusions list. Record the retry policy in the README, and do not let a timeout become a silent pass.
