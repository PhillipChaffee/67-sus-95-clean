# Duplication allowlist

jscpd on template configs can fire on repeated YAML/TOML boilerplate that is  intentional. Expect a per-language ignore list. Every ignore entry needs a reason, and the list must stay short.
