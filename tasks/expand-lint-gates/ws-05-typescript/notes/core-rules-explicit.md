# Core rules explicit

eslint core cap rules are NOT in any typescript-eslint preset. They are  core rules enabled explicitly. They work on TS sources through the typescript-eslint parser. Thresholds: eslint's `complexity` default is 20 the house bar is stricter: propose a threshold, make sure that against docs, record the reason.
