# Knip entry config

knip: needs entry-point and project config for the template layout  (src entry, vitest, the config files themselves). A wrong knip config flags everything or nothing: validate it reports the expected set on the example project before wiring CI.
