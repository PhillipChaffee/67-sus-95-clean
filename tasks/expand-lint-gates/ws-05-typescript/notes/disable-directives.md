# Disable directives

Suppression escalation: check the exact option name in the eslint docs at  the pinned version (reportUnusedDisableDirectives / linterOptions) and eslint reports unused disable directives at warn by default in recent versions, which passes their CI. Escalate to error so it fails.
