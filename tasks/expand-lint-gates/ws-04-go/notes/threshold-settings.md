# Threshold settings

Some linters need settings keys with thresholds (cyclop  max-complexity, gocognit min-complexity, funlen lines/statements, nestif min-complexity, mnd ignored-values, goconst min-occurrences, interfacebloat max-size). Propose strict-but-usable defaults, make sure that against each linter's docs, and record the reason next to each.
