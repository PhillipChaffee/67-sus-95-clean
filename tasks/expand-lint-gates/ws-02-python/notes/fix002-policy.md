# Fix002 policy

FIX002's current ignore reason says "TD001/TD003/TD004 already govern the  same comment". Removing the ignore is a policy change: the old reason must be replaced by the new one (uniform TODO ban), not deleted silently.
