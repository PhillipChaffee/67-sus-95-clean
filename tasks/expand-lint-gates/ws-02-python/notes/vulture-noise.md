# Vulture noise

vulture needs an allowlist (it flags dynamically-used names). Measure its  findings on a scratch repo first. The allowlist must stay short and every entry needs a reason. If the noise budget is worse than the signal, the fallback is to record vulture as an accepted gap: same refusal format as the epic's refusals.
