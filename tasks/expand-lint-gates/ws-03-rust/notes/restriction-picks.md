# Restriction picks

The rust folder's restriction picks are a shortlist by design ("choose YOUR  picks"). Adding allow_attributes_without_reason to the template's default list is a policy change: record why in the README table, not just config.
