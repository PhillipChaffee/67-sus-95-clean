{
  "//": "Threshold: fail when more than 5% of tokens are duplicated; ignore globs exclude the install-time byte copies (templates, ci.yml, run-gates.sh) and markdown, which the README-fingerprint check in verify-sync.sh owns.",
  "threshold": 5,
  "ignore": [
    "**/init-*-repo/templates/**",
    "**/ci.yml",
    "**/run-gates.sh",
    "**/*.md"
  ]
}
